import '../../css/inquiry/inquiry.css';

function InquiryView() {

    return (
        <div className='inquiry-container'>
            {/* <div className="inquiry-title">{subject}</div>
            <div>{nickname}<br/>{createdAt}</div>
            <div className="inquiry-img">
                {
                    imageList && imageList.length>0 &&
                    imageList.map(function(record) {
                        //const imgUrl = `http://192.168.1.252:9988/${}`
                        return (
                            <img    key={imageid}
                                    //src={`${}`} 
                                    //onClick={() => }        
                            /> 
                        )
                    })
                }
            </div>
            <div className="inquiry-content">{content}</div> */}
        </div>
    )
}

export default InquiryView;